# Frammenti generati da `ui_registry`

I file in questa cartella sono prodotti da:

```bash
python tools/sync_ui_registry.py --write
```

Non modificarli a mano: sovrascritti ad ogni sync. Per il processo maintainer vedi [`ui_registry/README.md`](../../ui_registry/README.md).
